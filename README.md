# Study Timer

A React single-page app for tracking study sessions by subject.

## Features
- Countdown timer with start, pause, stop and reset
- Configurable session length (default 25 minutes)
- User-managed subject list with add and remove
- Session history with per-session duration and timestamp
- Bar chart of total time spent per subject

## Running locally


## Built with
React (Vite)